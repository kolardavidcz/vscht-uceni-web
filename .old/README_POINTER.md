# Archived previous implementation

This folder holds the previous VŠCHT Učení SPA (source, docs, and domain content).

**Use for:** product intent, routes/features, brand rules, and **domain data** (wiki markdown, taxonomy, emoji catalog, Python script).

**Do not use for:** React components, custom parsers, or file layout as a template for the rewrite.

Active brief for the greenfield rebuild lives at the repo root: `FROM_SCRATCH.md`.
